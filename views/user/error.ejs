<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Error - Ocean Books</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Roboto', sans-serif;
      background: linear-gradient(135deg, #f0f2f5, #e0e7ff);
      color: #1C2526;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .error-container {
      max-width: 600px;
      text-align: center;
      animation: fadeIn 0.5s ease-in;
    }
    .error-icon {
      font-size: 80px;
      color: #e74c3c;
      margin-bottom: 20px;
    }
    h1 {
      font-size: 36px;
      font-weight: 700;
      color: #1C2526;
      margin-bottom: 15px;
    }
    p {
      font-size: 18px;
      color: #666;
      margin-bottom: 30px;
      line-height: 1.5;
    }
    .home-btn {
      display: inline-block;
      padding: 12px 24px;
      background: #58CC02;
      color: #FFFFFF;
      text-decoration: none;
      font-size: 16px;
      font-weight: 700;
      border-radius: 25px;
      transition: background 0.3s, transform 0.2s;
    }
    .home-btn:hover {
      background: #46a802;
      transform: scale(1.05);
    }
    .error-img {
      max-width: 100%;
      height: auto;
      margin-bottom: 20px;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
    @media (max-width: 768px) {
      .error-icon {
        font-size: 60px;
      }
      h1 {
        font-size: 28px;
      }
      p {
        font-size: 16px;
      }
      .home-btn {
        padding: 10px 20px;
        font-size: 14px;
      }
      .error-img {
        max-width: 80%;
      }
    }
  </style>
</head>
<body>
  <div class="error-container">
    <i class="fas fa-exclamation-circle error-icon"></i>
    <h1><%= status === 404 ? '404 - Page Not Found' : 'Oops, Something Went Wrong!' %></h1>
    <p><%= status === 404 ? 'Looks like this page got lost in the sea of stories. Let’s get you back to exploring!' : message %></p>
    <a href="/" class="home-btn">Back to Home</a>
  </div>
</body>
</html>