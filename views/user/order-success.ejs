<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ocean Books - Order Placed</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: #F5F5F5;
            color: #1C2526;
            text-align: center;
            padding: 50px;
            margin: 0;
        }
        h1 {
            color: #58CC02;
            font-size: 36px;
            margin-bottom: 20px;
        }
        p {
            font-size: 18px;
            margin-bottom: 30px;
        }
        .btn {
            padding: 10px 20px;
            background: #58CC02;
            color: #FFFFFF;
            border: none;
            border-radius: 25px;
            margin: 10px;
            cursor: pointer;
            font-size: 16px;
            font-weight: 700;
            text-decoration: none; /* Remove underline */
            display: inline-block; /* Fix alignment */
            transition: background 0.3s;
        }
        .btn:hover {
            background: #46a802;
        }
        .success-img {
            max-width: 250px;
            margin-bottom: 30px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
    <h1>Thank You for Your Order!</h1>
    <p>Your order #<%= order.orderId %> has been placed successfully.</p>
    <img src="https://cdn.dribbble.com/users/11783/screenshots/3492735/animation.gif" alt="Order Success" class="success-img">
    <div>
        <a href="/users/orders/view/<%= order._id %>" class="btn">View Order Details</a>
        <a href="/users/products" class="btn">Continue Shopping</a>
    </div>
</body>
</html>